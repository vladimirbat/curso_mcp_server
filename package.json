{
  "name": "tmdb-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server de ejemplo para consultar actores y peliculas mediante TMDB",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/tmdb.js && node --check src/errors.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  }
}
